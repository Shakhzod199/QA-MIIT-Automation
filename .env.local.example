# Copy this file to .env.local and fill in real values.
# .env.local is gitignored and never committed.

# GitHub Personal Access Token with Actions read/write (and Contents read)
# on the repo below. Used server-side only - never sent to the browser.
GITHUB_TOKEN=

# Repo that hosts the Playwright tests + GitHub Actions workflows
# (the TestExport project itself, once pushed to GitHub).
GITHUB_OWNER=
GITHUB_REPO=

# Dashboard login (stored in env, no database needed)
DASHBOARD_USERNAME=Shakh199
DASHBOARD_PASSWORD=shakh199
DASHBOARD_SECRET=change-this-to-a-random-secret
